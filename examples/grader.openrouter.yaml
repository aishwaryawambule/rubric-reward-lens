# Real LLM grader via OpenRouter. Needs OPENROUTER_API_KEY in your environment.
# Swap `model` for any OpenRouter slug (e.g. openai/gpt-4o-mini, google/gemini-2.0-flash).
type: openrouter
model: anthropic/claude-haiku-4.5
temperature: 0.0
